#include <stdio.h>
#include "math_.h"

int main() {
    int a = 10;
    int b = 5;

    printf("Add: %d\n", add(a, b));
    printf("Subtract: %d\n", subtract(a, b));
    printf("Multiply: %d\n", multiply(a, b));
    printf("Divide: %d\n", divide(a, b));
    printf("Modulo: %d\n", modulo(a, b));

    return 0;
}




























